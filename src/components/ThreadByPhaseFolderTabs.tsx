/**
 * @deprecated Import `ThreadByPhaseSection` from `@/components/ThreadByPhaseSection`.
 * Kept so stale dev-client bundles can still resolve this module after the rename.
 */
export {
  ThreadByPhaseSection as ThreadByPhaseFolderTabs,
  type ThreadByPhaseContent as ThreadByPhaseSection,
  type ThreadByPhaseBlock,
} from "@/components/ThreadByPhaseSection";
